#ifndef MATH_LIB
#define MATH_LIB

namespace mb {
using value_type = int;

value_type add(value_type, value_type);
value_type mul(value_type, value_type);
value_type div(value_type, value_type);
value_type exp(value_type, value_type);
}  // namespace mb

#endif
